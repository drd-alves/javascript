function atualizaNome(){

    var nometrocado = prompt("Digite seu nome:")
    // var nome = document.getElementById("nome").textContent
    // var nome = nome.split(",").join(""); nome = nome.split(" ")
    // var nometrocado = prompt("Digite seu nome: ") 

    // for(i=0; i<nome.length; i++){
    //     nome[i] = nome[i].replace(nome[1],nometrocado)
    // }

    nome.textContent = "Olá " + nometrocado + ", como vai?" 
    
    // console.log(nome)

} 